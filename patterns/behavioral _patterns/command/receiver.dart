abstract class Receiver {
  Set<String> get actions;
}

/**
 * Set representa uma coleção de objetos 
 * em que cada objeto pode ocorrer apenas uma vez.
 *  A biblioteca dart: core fornece a classe Set para implementar o mesmo.
 */
